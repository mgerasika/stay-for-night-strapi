{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "Order",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "request": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::article.article",
      "mappedBy": "order"
    },
    "volunteer": {
      "type": "relation",
      "relation": "oneToOne",
      "target": "api::volunteer.volunteer",
      "inversedBy": "order"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "waiting",
        "processed",
        "done"
      ]
    }
  }
}
